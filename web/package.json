{
  "name": "web-viewer",
  "version": "1.0.0",
  "description": "A minimal web viewer for processed frames.",
  "scripts": {
    "build": "tsc"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
